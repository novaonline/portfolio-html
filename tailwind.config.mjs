export default {
  darkMode: "class",
  content: ["./src/**/*.{astro,html,md,mdx,js,ts,tsx,vue}"],
  theme: { extend: {} },
  plugins: [
    // Optional: add typography if `prose` is used; leave TODO if not installed
    // TODO: enable `@tailwindcss/typography` once the dependency is added
  ],
};
